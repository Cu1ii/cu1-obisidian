1. springBean 以后建议使用**构造函数注入**，字段的初始化也放在构造函数中，方便单元测试
2. ~~MyBatis 注册全局 configration 不要添加 typeHandler，会覆盖指定字段的 typeHandler~~    
3. 不建议使用 lombok 的 `@UtilityClass` 注解，会让人产生困惑。
4. mysql `select for update` 方式加分布式锁，使用唯一索引/主键索引